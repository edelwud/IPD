cmake_minimum_required(VERSION 3.17)
project(Keylogger)

set(CMAKE_CXX_STANDARD 14)

set(KEYLOGGER_INCLUDE_DIR "${CMAKE_CURRENT_SOURCE_DIR}/include")
set(KEYLOGGER_INSTALL_INCLUDE_DIR "${CMAKE_INSTALL_INCLUDEDIR}")
set(KEYLOGGER_INSTALL_LIB_DIR "${CMAKE_INSTALL_LIBDIR}")

add_subdirectory(src)